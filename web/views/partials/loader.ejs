<% if (loading) { %>
    <div id="page-loader" class="fixed inset-0 z-50 flex items-center justify-center bg-white/30 backdrop-blur-md">
        <div class="relative flex items-center justify-center">
            <img src="/assets/logo.png"
                class="relative z-10 w-1/3 h-1/w-1/3 sm:w-1/2 sm:h-1/2 object-contain animate-pulse" alt="Loading" />
            <div class="absolute inset-0 flex items-center justify-center">
                <div
                    class="w-46  h-46 sm:w-72 sm:h-72 rounded-full border-4 border-blue-100 border-t-blue-500 animate-spin">
                </div>
            </div>
        </div>
    </div>
    <% } %>